using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class test : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        Vector2 initial = new Vector2(2,3);
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
